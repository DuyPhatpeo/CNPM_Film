@model IEnumerable<ProjectFilm_CNPM.Models.ERD.BaiViet>
@{
    ViewBag.Title = "Thùng rác bài viết";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
    var i = 1;
}

@section header{
    <link href="~/Public/css/jquery.dataTables.min.css" rel="stylesheet" />
}
@section footer{
    <script src="~/Public/js/jquery.dataTables.min.js"></script>
    <script>let table = new DataTable('#myTable');</script>
}
<div class="wrapper">
    <!-- Main Sidebar Container -->
    <!-- Content Wrapper. Contains page content -->
    <div class="content-wrapper">
        <!-- Main content -->
        <section class="content">

            <!-- Default box -->
            <div class="card">
                <div class="card-header">
                    <div class="row">
                        <div class="col-md-6">
                            <strong class="text-danger">THÙNG RÁC BÀI VIẾT</strong>
                        </div>
                        <div class="col-md-6 text-right">
                            <a href="@Url.Action("Index","BaiViet")" class="btn-sm btn-info"> <i class="fa fa-long-arrow-alt-left"></i> Quay lại</a>
                        </div>
                    </div>
                </div>
                <div class="card-body">
                    @*hien thi dong thong bao*@
                    @Html.Partial("Message")
                    <table class="table table-bordered table-striped" id="myTable">
                        <thead>
                            <tr>
                                <th>Số thứ tự</th>
                                <th>@Html.DisplayNameFor(model => model.ChuDe.TenChuDe)</th>
                                <th>@Html.DisplayNameFor(model => model.TenBV)</th>
                                <th class="col-md-1">@Html.DisplayNameFor(modelItem => modelItem.Anh)</th>
                                <th>@Html.DisplayNameFor(model => model.KieuBV)</th>
                                <th>@Html.DisplayNameFor(model => model.TuKhoa)</th>
                                <th>Chức năng</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {

                                <tr>
                                    <td>@i</td>
                                    <td>@Html.DisplayFor(modelItem => item.ChuDe.TenChuDe)</td>
                                    <td>@Html.DisplayFor(modelItem => item.TenBV)</td>
                                    <td><img src="~/Public/img/baiviet/@item.Anh" alt="Img" class="img-fluid" /></td>
                                    <td>@Html.DisplayFor(modelItem => item.KieuBV)</td>
                                    <td>@Html.DisplayFor(modelItem => item.TuKhoa)</td>
                                    <td>
                                        <a href="@Url.Action("Undo","BaiViet", new { id = item.Id })" class="btn-sm btn-info"> <i class="fa fa-undo"></i></a>
                                        <a href="@Url.Action("Delete","BaiViet", new { id = item.Id })" class="btn-sm btn-danger"> <i class="fa fa-trash"></i></a>
                                    </td>
                                    @{ i++;}
                                </tr>

                            }
                        </tbody>

                    </table>

                </div> <!-- /.card-footer-->
            </div><!-- /.card -->
        </section><!-- /.content -->
    </div> <!-- /.content-wrapper -->
</div><!-- ./wrapper -->
